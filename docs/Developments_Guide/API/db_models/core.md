# Core Models

The core module provides the `BaseModel` class, which serves as the foundation for all other database models in the system.

::: automation.dbmodels.core.BaseModel
    :docstring:
    :members: read
    :members: read_all
    :members: put
    :members: id_exists
